package dev.desktop;
import java.util.*;

public class compiler {
    String input;
    public compiler(String input) {
        this.input = input;
        lex(input);
    }
    private void lex(String input) {
        // todo
    }
}